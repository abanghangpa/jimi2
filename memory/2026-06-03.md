## 2026-06-03: Scanner and Proxy Investigation

### Scanner Hang Issue
- The Jimi Audit scanner (`scripts/scanner.py`) hangs during data fetching, likely due to network/API calls in:
  - `src/utils/web_data_utils.py` (yfinance for TradFi data, duckduckgo for web search)
  - `src/utils/data_handler.py` (ccxt for Binance data)
- Recent successful runs: 
  - 2026-06-03 08:00:00 (scan_20260603_081337.json)
  - 2026-06-03 13:45:00 (scan_20260603_134958.json)
- Cron job `Hourly Scanner` (every 30m) times out due to isolated agent setup taking too long.

### Free‑Tier Proxy Configuration
- Updated `fallback_proxy.py` to map OpenClaw model IDs (e.g., `gpt-5.5`) to the upstream‑supported model (`gemini-2.5-pro`).
- Proxy now has internal fallback: free service → production Gemini key (`PROD_API_KEY`).
- OpenClaw model routing (`openclaw.json`) prioritizes free tier with multiple retry attempts before falling back to NVIDIA then Gemini/Gemma.
- Current issue: both free and production keys are returning 429 (rate limit/quota exhausted).

### Recommended Actions
1. For scanner: increase timeouts, check network connectivity, or run lighter version.
2. For proxy: rotate free keys via `rotate_keys.py` (cron every 20m) and verify Gemini quota.